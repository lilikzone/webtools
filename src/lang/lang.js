import LocalizedStrings from 'react-localization';
import en_lang from './en.js';
import id_lang from './id.js';

const strings = new LocalizedStrings({
  en: en_lang,
  id: id_lang,
});

export default strings;
